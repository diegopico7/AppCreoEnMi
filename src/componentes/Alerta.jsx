import React from "react";

const Alerta = () => {
  return (
    <div>
      {alert(
        "Para una mejor experiencia recuerde que esta app esta optimizada para dispositivos móbiles, pronto optimizaremos para dispositivos de escritorio. Muchas Gracias"
      )}
    </div>
  );
};

export default Alerta;
